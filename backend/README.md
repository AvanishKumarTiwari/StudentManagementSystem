# Student Management System - Backend

This backend provides RESTful APIs for the Student Management System.

Getting started:

1. Copy `.env.example` to `.env` and set your MongoDB URI and JWT secret.
2. Install dependencies: `npm install`.
3. Run in development: `npm run dev` (nodemon).

APIs are mounted under `/api/*`.
